/*
	Sockets - provide a solution for realtime chat system. because they provide a bi-directional communcation channel between client and server. The server is able to PUSH messages to the client.  The client writes a msg, the server gets it, and the server pushes it to ALL the other connected clients.
 */